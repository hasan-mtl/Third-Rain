Design loop - world-class front-end, every 20m until 2026-06-12 07:04 +0800 (cron 29808b7e). Protocol: .claude/design-loop.md

1. micro-interactions - boat hover: word eases +5% larger, halo +0.13, fill to 1.0, seat glow lifts; smooth 9/s lerp (never snaps), rect hit-test now index-based, cursor cleanup on leave
2. typography rhythm - section ledes capped to a true reading measure (48ch -> 557px ≈ 64 avg chars, was ~85), centered; section h2 gets text-wrap: balance (no widows on two-line headings). Verified computed: 557px x3 ledes, balance applied, overflowX 0.
3. spacing laws - section rhythm unified by measurement: flow padding-top 84 -> 116 (every headed section now starts at 116), demo head margin 30 -> 38 so its OPTICAL head-to-content gap = 56px exactly like services/process (was 48 vs 56). Verified live: 56/56/56 across all three, flow pt 116px, overflowX 0.
4. motion polish - reveal system now RETIRES after entrance (.is-done at i*90+820ms): the transition shorthand + stagger delay no longer override revealed elements own hover/focus transitions (chips bg fades restored, no more 540ms hover lag on late-stagger cards); entrance itself upgraded to split-track easing (opacity 0.55s ease-out leads, transform 0.75s settles). Verified: 12/12 revealed elements retired, computed delay 0s post-reveal, stagger intact.
5. a11y - keyboard path to services restored (lost in v34 when live mode display:none-d the chips): chips stay in the tab order, invisible at rest (opacity 0, nav pointer-events none so boat clicks pass through), each pill fades in on :focus (safe here: pointer can never focus them, so no heuristic needed) with the global focus ring; Enter navigates. Verified: rules in served CSSOM, resting opacity 0, 6 focusable links, activeElement lands; :focus visual is window-focus-gated (preview limitation, spec-guaranteed in real browsers).
6. performance - scene dependency warmup: static preconnect to cdn.jsdelivr.net (kills DNS+TLS at import time) + desktop-only modulepreload of three.module.min.js injected at requestIdleCallback from inside the engine block (mobile/reduced-motion never pays the 150KB; module cache is hot before IO triggers the import -> ocean starts instantly). Verified: 3 preconnects, modulepreload present w/ crossorigin anonymous, v47 live.
7. responsive - audited flow at 390/768/1024 (+1440 prior): no overflow anywhere, chips wrap to 2 tidy rows at 390, mark centered, boats all in-frame at 1024 w/ depth clamp active. FOUND+FIXED: mobile (<901) fallback was the wordmark on a FLAT dark box - no water at all for phone traffic; added a pure-CSS night-ocean gradient (horizon band at 52%, water graying toward the page) in the scene palette, zero JS, motion-free. Verified: gradient computed at 390 (8 stops), absent at 1280, overflowX 0 both.
8. the scene - the rain breathes: scene rainfall alpha swells 0.82->1.0 on a ~27s sine (matches the horizon-glow breathing register); hero rain untouched. One multiplier, console clean, engine live.
9. micro-interactions (rotation 2) - one press language across the interactive set: .demo-chip:active scale(0.96), .cap-btn:active scale(0.985), .basin-chip:active scale(0.97), each with transform 0.12s on the site curve joined to their transition families (.btn already had translateY(1px)). Presses now acknowledge the finger everywhere, consistently. Verified in served CSSOM + computed transition includes transform; overflowX 0. (Enabled by design 4 - reveals no longer hijack these transitions.)
10. typography rhythm (rotation 2) - numeric hardening: .result__view inherits font-variant-numeric: tabular-nums, guaranteeing steady digits across ALL morphing views (PO table cells, delta chips, timestamps), not just the strong counters that already had it. Audit note: serif-grad tracking + counter tabular were already correct (earlier truncated grep misled - verified properly this round). Verified: computed tabular-nums on view + inherited em, overflowX 0.
11. spacing laws (rotation 2) - held, no change: audited the unvisited structures and the law holds everywhere - caps columns share one bottom edge (2814/2814), demo chat/result equal (355/355), footer brand/nav/meta centers Δ0.0px, platform/contact custom layouts deliberate. Nothing safe to improve without inventing work.
12. motion polish (rotation 2) - result-card morph timing split: outgoing view exits in 0.22s, incoming enters over 0.45s delayed 0.08s (was 0.4/0.45 simultaneous = two ghosts stacked for ~400ms). Views now hand over crisply. Verified computed (0.22s exit / 0.08s enter delay) + full scenario path still morphs (post -> order at 1.5s via real chip click).
13. a11y (rotation 2) - the demo theater now speaks properly: the self-mutating simulation (chat typing, morphing card) is aria-hidden (constant DOM churn would be hostile narration), replaced for AT by one sr-only story sentence before the stage; scenario chips stay real toggles (button + aria-pressed already present; auto-cycle already reduce-gated). Added the missing .sr-only utility. Verified: grid hidden, sr text 1x1px offscreen (293 chars), chips real, overflowX 0.
14. performance (rotation 2) - decorative loops sleep off-screen: one IO (rootMargin 14%) toggles .is-offstage per section/ticker, pausing aurora ::before drift, motes, ripples, float-chips, ticker marquee, circuit spin/dash via animation-play-state. At page top 6/7 sections sleep; wake is pre-entry and seamless (alternate anims resume in place). Verified live: paused computed off-screen, running after scroll, all sections gate correctly.
15. responsive (rotation 2) - hero audit at 390: no overflow, h1 symmetric, stats equal thirds, tap targets 50px, video correctly gated. FOUND+FIXED: the two CTAs wrapped into a ragged intrinsic-width stack (192px over 151px); <=520px they now stack full-width with equal weight (both 350x50 flush at x20). Desktop intact (same row at 1280).
16. the scene (rotation 2) - one shared breath: the rainfall swell factor hoisted to bBreath and the wordmark water-reflection now rides it too (alpha 0.07+0.05*breath, range ~0.11-0.12 peak vs old flat 0.10) - when the rain swells, the reflected light lifts with it; the scene breathes as one organism. Console clean, engine live.
17. micro-interactions (rotation 3) - the contact form says "no" physically: rejected fields head-shake 0.3s (re-triggerable via reflow restart, repeat submits shake again), reduced-motion guarded; clears with the invalid state on input. Verified live: empty submit -> shake + aria-invalid + focus to first invalid; typing clears.
18. typography (rotation 3) - tracked-uppercase ladder corrected: the smallest labels (10.5px THE PAIN/THE FIX chips + circuit nodes) were the LEAST tracked (0.12em, inverted optics); now 0.17em -> absolute tracking ~1.7-1.8px constant across 10.5/11/12px = perceived spacing uniform. Verified computed (1.785/1.76/1.68px), no clipping (scrollWidth==width on all nodes), overflowX 0.
19. spacing (rotation 3) - held, no change: card-padding families coherent (cap-panel 28, form 32, console mat 8 sanctioned), chat rhythm flex-gap uniform, header/footer bookends IDENTICAL containers (1120 maxw, 24px pad - initial 24px "mismatch" was my own container-vs-content misread, corrected), caps grid card-bleed deliberate. The spacing system keeps passing; refusing to invent work.
20. motion (rotation 3) - hero outcome-cycle polish: swap now sleeps when the hero is offstage (reuses the design-14 class - no more invisible churn + you never return mid-blur), and the swap got the exit-quick/enter-considered split (sink 0.26s, rise 0.4s, JS swap point 270ms). Verified live: swaps while visible, frozen 4.2s while scrolled away, 0.4s idle transition computed.
21. a11y (rotation 3) - honest semantics: role=tablist removed from caps__list + demo__chips (they are correctly-implemented aria-pressed toggle groups, not tabs - tablist promised arrow-key semantics that did not exist) -> role=group with labels; scrollspy now mirrors aria-current=true onto the active nav link (set/cleared on the same lines as is-active). Verified: zero tablists, 2 labeled groups; aria-current structurally paired with the visual state (frozen-tab IO cannot demo the spy itself - documented limitation).
22. performance (rotation 3) - content-visibility: auto on #process + #contact (contain-intrinsic-size: auto 700px): the browser skips their full style/layout/paint until approached, and the auto keyword locks in real sizes after first render. Verified: applied, both render on scroll at pixel-identical heights (610/799), docH unchanged 6394 = zero CLS, overflowX 0.
23. responsive (rotation 3) - demo at 390 healthy (stacked, both cards fit one viewport, chips wrap, zero overflow), drawer healthy (61px links, aria-expanded clean) EXCEPT the menu toggle itself: 40px, under the 44px tap floor - the most-tapped mobile element. Fixed via ::after inset -4px: visual stays 40px, hit area 48px. Verified: edge click 2px outside the visual lands on the toggle, drawer opens/closes clean.
24. the scene (rotation 3, final working iteration) - the sky joins the breath: glyph alpha now rides bBreath (0.72+0.28*breath) alongside the rainfall and the wordmark reflection - all three ambient layers swell as one organism on the ~27s cycle. Verified: node-checked, v63 executes with zero console errors, bBreath same-scope as design 16 (proven live); harness IO refused to wake the engine this round (documented limitation, change is a clamped one-multiplier alpha).
25. held - window edge (07:01, deadline 07:04): no improvement can be verified and shipped inside 3 minutes; refusing a rushed change as the closing act.

LOOP COMPLETE - 2026-06-12 07:04 +0800. 25 fires over 8 hours: 21 shipped improvements (v42-v63), 3 honest holds, 1 window-edge close. Every ship verified to the limit the harness allowed; production never broke. Focus rotation completed 3 full cycles: micro-interactions, typography, spacing, motion, a11y, performance, responsive, scene.
- Mike directives (2026-06-12): RESTRUCTURE (v64).
25b. THE OCEAN IS THE HERO - old video hero deleted (video/console/stats/rain-fx all gone, JS no-ops via guards); the scene is section 1 at 100svh with asymmetric copy (badge + cycling H1 + sub + CTAs top-left, wordmark re-anchored to 0.68 right, fleet re-biased right of the copy column, GLSL bloom follows); ticker is the hero, fold strip. Order now hero>services>demo>platform>process>contact. PLATFORM promoted above process + bigger stage (circuit 520->640, padding 148). ALL GRIDS DEAD: circuit-grid/dot-grid elements + rules deleted, warm-grid squares -> soft radial washes (Mike hates grids). CONTACT rebuilt as a chat with rain: glass card, avatar header + Online pill, incoming bubble, name/email row, chat composer textarea + plane send (all ids/data-attrs kept - validation/shake/mailto verified live: empty submit -> shake+aria+note+focus; success path -> mailto note). Mobile: copy centered, CSS-ocean fallback, hero 648px (cascade specificity fix), chat 1-col, overflowX 0 at 390+1380.
25c. hero cleanup after Mike screenshot (v65) - badge DELETED, both in-scene CTAs DELETED (they straddled the waterline; header carries the action), the giant wordmark REMOVED from the hero (header owns the brand; particle sampling skipped, reflection no-ops, GLSL sky bloom deleted), glyphs 10->6 at 0.55 alpha (atmosphere not debris), copy CENTERED (clamp top 15vh, H1 up to 4.2rem), fleet re-laid on a screen-space grid - collision sweep at 1909px across 3 drift phases: worst pair clears 62px (Branding~Software; was overlapping). Verified at Mike viewport 1909x962: centered, live, no overflow, screenshot = clean cinematic hero.
25d. the rain returns (v66, Hasan: "the coolest thing - bring it back") - the particle wordmark is back as the CREST: assembles centered high in the sky (0.12bW cap 170, baseline 0.245bH, ~2200 drops), sky bloom restored behind it (0.5/0.2 fixed-fraction), reflection still bends on the water; copy moved beneath it (top 31vh, H1 3rem, one-line sub w/ mobile unwrap). Measured at 1909x962: 47px crest-to-copy gap, sub floats 16px onto the far water, 12px clear of the farthest boat (drift moves it ~1px). Lesson logged: the wordmark was never the problem - the off-center placement was.
25e. crest enlarged + wording (v67) - sub-line DELETED per Mike screenshot; crest +32% (0.155bW, caps bH*0.3/225px, baseline lifts on short screens via max(0.25bH, font*0.78+28)) and HIGHER (top 45px @1909x962, bloom 0.17bH); wording -> present tense: "While you sleep," + cycles "your blog posts itself / report arrives at 8:00 / every customer gets a reply / low stock reorders itself" (ongoing promise, not anecdote). Copy ends +3px at the waterline, 57px under the crest. Verified live geometry, overflowX 0.
25f. headline lifted (v68, Mike circle+arrow) - copy top clamp(264,34vh,340) -> max(258px, 30.5vh): sits 23px under the crest, 30px above the waterline, 58px clear of the far boat (was ON the horizon with Branding colliding beneath). Verified at 1909x962.
25g. platform restaged (v69, Mike circle: "redesign, mark thing center, rest after, premium") - new layout: centered section-head -> THE CIRCUIT as the stage (820px centered, was 640 side-column) -> 4 tick-chips feature strip (one row @desktop, glass pills) -> stats row -> run log belt (720 centered) -> centered CTA pair. platform__inner/copy/viz two-column layout deleted. Mid-surgery the extraction helper sliced an unterminated fragment (no end-assert) and ATE the section close - caught by DOM-children probe, reset to HEAD, redone with asserted cuts + div-balance checks. Verified: child order head>stage>strip>stats>belt>actions, all centered, 6 pulses, order intact, overflowX 0.
25h. wording final (v70, Hasan) - H1 = "While you were sleeping…" (typographic ellipsis), cycles back to past tense (the night happened): blog posted itself / report arrived at 8:00 / every customer got a reply / low stock got reordered. Verified live render + geometry holds (copy -8px vs horizon), overflowX 0.
25i. ledes spread (v71, Mike: "hates contained paragraph style") - section-head 720->880, lede measure 48ch->66ch, platform head 1060 w/ uncapped lede. ALL four section ledes now render as ONE wide line (766/766/1060/766px @1380). Taste rule recorded: ledes spread wide, never narrow reading columns.
25j. platform rhythm (v72, Mike: "bad spacing, no match with top") - the circuit aspect-box carried 78px of dead space below its lowest node -> visual gap to the strip was 137px vs the 61px section reference. Folded away with margin-bottom: -9.5% on the circuit (percentage = scales with circuit width), actions 40->42. Measured rhythm now 61/59/42/42/42 - two clean tiers, no overlap, overflowX 0.
25k. platform air (v73, Mike arrows) - circuit TOP dead zone folded too (margin-top -8.5%): lede->first tile 134 -> 64px; section pad-top 148->124; after the CTA row 24 -> 64px (+40 as ordered; padding 64 + 18 actions margin after measuring a 18px swallow). Verified 64/64, overflowX 0.
25l. em dashes banned (v74, Hasan permanent rule, all projects). 198 removed across 11 files. Visible copy rewritten properly (colon for the platform lede, the middle-dot for the founder chip, periods elsewhere); comments and logs swept mechanically. Saved to permanent memory: never use an em dash for Hasan again, anywhere, including commits.
26. CLIENT DESIGN-FORK IMPORT (v75) - merged Ether/Mike Claude Design fork into the live site (Hasan: apply all recent changes; chose Storm hero). Brought in: STORM editorial-split hero (left eyebrow/headline/sub/2 CTAs/credentials over heavy rain-only scene, particle wordmark right at markX0.7 via __heroTweak noOcean+rainMul2.3+markScale1.4); new STATEMENT beat section; 4 WebGL water section-merges (merge-water.js, all 4 canvases live 1369x290); SOLAR-circle platform core (clock + 2 orbits, 8 nodes); MacBook-window demo screens; 3-card process timeline; particle header logo; ed-head editorial section heads; de-blued paper; refreshed demo scenarios (no alcohol: social/content/restock/replies). Files: adopted fork index.html + rain.js (superset of mine + storm/dawn/coupling), added rain-editorial.css/hero-options.css/rain-tweaks.js/merge-water.js, asset coldbrew-hero.png; rain.css identical to mine (kept). Purged ALL 45 em-dashes (permanent rule), unified cache to v=75. Verified: order hero>statement>services>demo>platform>process>contact, engine live, 4 water canvases live, 0 console errors, 0 em-dashes in DOM, overflowX 0, Storm hero screenshot clean.
27. statement headline 2 lines + cache bust (v76) - Mike: statement headline wrapped to 3 lines on wide screens (96px font in a 1120px box). Widened .statement__inner to min(1360px, 100vw-48px) so the big display headline fits 2 lines from ~768px up, capped so it never stretches to 1 line on ultrawide; verified 2 lines at 1440 + 1920, overflowX 0. Also bumped all assets v75->v76 to bust the stale browser cache that was showing the principal the pre-purge dash AND the old demo result (verified live demo switching already works: clicking Manage Inventory shows the Purchase Order view correctly).
28. unified section rhythm (v77, Mike: padding inconsistent, big in some/small in others, reduce). Root cause: water-merge sections reserved 300px + 290px bands while paper sections sat at 18/124px - wildly uneven. Normalized via --water-room 176 / --water-band 168 (was 300/290) applied to ALL 4 merges (statement pb, demo pt, platform pb, contact__inner pt); paper sections .caps/.process symmetric 48/48 (was 18/124); dark-dark demo->platform 56/56; statement pt clamp 56-72 (was 64-104). Verified live @1440: rhythm hero64/0, statement60/176, services48/48, demo176/56, platform56/176, process48/48, contact inner176; all 4 water canvases live 1429x168; overflowX 0.
29. statement headline reduced (v78, Mike: reduce font size). .statement__line clamp(2.6rem,7vw,6rem) -> clamp(2rem,4.6vw,4.2rem): 96px -> 66px @1440 (31% smaller), line-height 1.04->1.06. Still 2 lines, overflowX 0.
30. demo real-time fix (v79, Mike: "not updating real time, 2 seconds slow"). Bug: chip click ran runScenario which highlighted the chip instantly but deferred the reply + showView to the 1500ms typing-theater timer, AND chatMsg kept 5 msgs so scenarios piled up (clicked "Post my blog" while result still showed prior Purchase order). Fix: runScenario(key, immediate) - user taps resolve instantly (clear chat, user+agent msg, showView now); typing theater kept only for autoplay; chatBody cleared each scenario so exchanges never mix. Verified: all 4 chips sync result-view+chip+2msgs within ~1 frame (restock->order, content->post, social->social, replies->inbox).
31. crown-jewel pass (v80, Mike: bigger futuristic clock + 25x premium services, kill retired radius). CLOCK: 36->52px (disk 116->130), rebuilt SVG as a precision instrument - radial-gradient face, fine 60-tick + major 12-tick bezels, gradient hour/minute hands (url stroke), a sweeping 6s second hand w/ glowing tip, hub + hub-ring, drop-shadow glow; richer disk gradient + stronger teal glow. SERVICES: killed the retired radius (cap-btn 16->12, cap-panel 2rem/32->16), cap-btn now gradient-surface + hairline + lift-on-hover + glowing gradient accent rail on active; cap-panel gets a gradient hairline border (sky->teal padding/border-box) + premium layered shadow. Verified: clock 52/disk 130, gradient hands live, sec animating, radii 12/16, console clean, overflowX 0.
32. services readability (v81, Mike: content font bigger, hard to read). Bumped the services section content: card title 14.5->16, card subtitle 12->13.5, number badge 11->12, panel h3 23->24, panel subtitle 13->15, pain/fix labels 10.5->11.5, the pain sentence 14->16, the HOW-RAIN-FIXES checklist 14.5->16, outcome band 13.5->15. Verified: all sizes applied, panel not clipped, 6 cards still equal-height (77px), overflowX 0.
33. demo step-labels moved to bottom (v82, Mike: the You-ask/Rain-does-it row after the chips is confusing). Removed the two .demo__cap headers (windows now sit 38px directly under the task chips - no confusing middle row). Rebuilt the redundant bottom figcaptions into one polished step badge each: gradient rounded-square icon (sky->blue / teal->emerald) + "You ask"/"Rain does it" title + description, in a subtle glass pill, centered under each window. Connector pulse re-tuned 53%->41% for the higher windows. Verified @1440: caps gone, 38px gap, badges centered (offset 0), connector within 9px of window mid, overflowX 0; @390: badges fit, no overflow.
34. clock centered + creative "always on" + alternate Pick-a-problem (v83, Mike). CLOCK: removed the stacked "rain agent/always on" text from the disk so the clock now sits at the EXACT center of the orbits/disk (verified centered); clock 52->56; the brand is shown a NEW way - "· RAIN AGENT · ALWAYS ON ·" engraved on a circular SVG textPath that slowly rotates around the clock like a watch bezel (38s); inner orbit removed to make room; richer disk. ALTERNATE SECTION: built a 2nd creative "Pick a problem" right after the original (both visible to compare) - a CSS-only (radio-driven, zero JS, no conflict w/ the existing JS selector) horizontal numbered tab strip + a split-stage panel (left = identity + dramatic 17px pain; right = fix checklist + outcome), gradient-edged. Content auto-extracted from the 6 existing panels. Verified @1440: clock centered+seal spinning, order hero>statement>services>c2-sec>demo..., 6 tabs each switch to the right panel, old selector intact, console clean, overflowX 0; @390: tabs wrap 2/row, panel stacks, clock still centered, no overflow.
35. real clock + auto-tabs + bigger seal (v85, Mike). CLOCK is now a REAL clock: JS sets the hour/minute/second hands to the correct local time and ticks once per second (aligned to the whole-second boundary); removed the fake CSS ra-spin from the hands. Verified hands match wall time (7:01 -> hr 210.5deg, min 9.4deg) and tick (sec 342->6->30). Seal "RAIN AGENT ALWAYS ON" bezel text 6.2->7.6px, brighter. ALTERNATE SECTION: tabs now AUTO-ADVANCE every 3.6s (robust: interval always runs, IO only sets a visible flag so it works even when IO is frozen; pauses on hover; syncs on click) with a fade-in per panel. Verified: tabs cycle 0->1->3, pause on hover, console clean, overflowX 0.
36. CLOCK ACTUALLY FIXED (v86, Mike was right). The "real clock" in v84/v85 was NOT working: a leftover duplicate rule (.ra-clock__hand--h/m { animation: ra-spin }) from the original fork CSS at lines 479-480 was STILL applying the fake spin, overriding the JS real-time transforms (CSS animation beats inline style). My v85 verification only read el.style.transform (the JS value) not the COMPUTED matrix, so it missed that the render was still the spin. Removed the leftover animations. Verified via COMPUTED transform matrix angle == expected for the wall clock: 19:17 -> hour rendered 219 (exp 219), minute 105 (105), second 162 (162), ticking true. Lesson: for transform correctness, check getComputedStyle transform matrix, not just element.style.
37. alternate header matches the original (v87, Mike). c2-sec header reworded to be identical to #services: eyebrow "What we fix", h2 "Pick a problem. Watch it disappear.", lede "Six ways businesses leak time and money, and how we fix each one." So the comparison is purely about the layout/design, same copy on both. Verified c2 header text matches, overflowX 0.
38. delete social-post droplet tile (v88, Mike selected it). Removed the .social-post__media tile (droplet on gradient) from the demo social result; the caption now sits clean full-width. Verified mediaGone, caption intact, overflowX 0.
39. premium watch + single seal (v89, Mike selected the orbit). SEAL: "RAIN AGENT · ALWAYS ON ·" now appears ONCE around the ring (was 3x), bigger (7.6->9px), still rotating (38s). WATCH made more premium + real: thin line hands replaced with tapered dauphine polygon hands (gradient steel-blue hour / steel-teal minute, drop-shadow depth), refined dial (bright bezel ring + faint inner ring + 60 fine ticks + 12 brighter hour ticks), a thin counter-weighted second hand with a glowing lume tip, polished white hub + cyan hub-ring. Verified: still tells correct local time with the new polygon hands (19:33, hour/minute angles exact), seal repeats=1 at 9px, console clean, overflowX 0.
40. seal redo (v90, Mike: dot on the text line, text too small/thin/spaced, dislike). Removed orbit-a entirely (the dashed ring + its running planet dot that tracked across the text). Moved "RAIN AGENT · ALWAYS ON ·" off the big outer ring onto a TIGHT dial ring (path radius 76->40, close to the clock) so the words pack tightly instead of stretching around the whole circle; font 9->12px, weight 500 + paint-order stroke (FAT), bright near-solid fill, z-index 2->4 (above the disk, on the dial, clearly visible), still rotating (30s). Clock still correct, console clean, overflowX 0.
41. expand the watch (v91, Mike). Scaled the whole central watch up ~29%: core container 168->190, disk 116->150, clock 56->74, ripples 108->138, seal SVG 168->190 (dial text scales with it, path radius 40->50, font 12->13). Core is absolute-centered (translate -50%) so traces stay aligned; nodes unaffected. Verified: sizes applied, clock still centered + correct time, console clean, overflowX 0.
42. active tab color + seal de-fat (v92, Mike). Alternate-section active tab is now a distinct blue gradient (var(--blue-500->600)) with white title + translucent-white icon tile, so the selected tab is instantly obvious vs the white inactive ones (was white-on-white, barely distinguishable). Seal "RAIN AGENT" text: removed the paint-order faux-bold stroke (too fat), kept weight 500 + slight letter-spacing for a clean medium inscription. Verified: active tab gradient distinct from inactive, white title, seal stroke gone/weight 500, overflowX 0.
43. clock ripple clip fix (v93, Mike: edge cut when rippling). The clock SVG (viewBox 48) clips to its viewBox by default, so the ra-clock__ping sonar pulse (scales to 1.35x = r27 > r24 viewBox) got cut square at the box edges. Set overflow: visible on .ra-clock so the pulse expands as a full circle onto the dial. Verified overflow visible, ping animation intact, clock correct, overflowX 0.
44. rain agent intro + hero merge - platform leads with big "Introducing Rain Agent" + one-line subline; two CTAs collapsed to one XL "Start automating your business"; hero dissolves seamlessly into statement (rain mask 77%, hero floor fades to #070d1c statement color, light pool on seam). v=94
