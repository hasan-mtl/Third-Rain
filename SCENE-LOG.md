# Scene loop log
started: 2026-06-11T19:26:31+0800
cadence: every 10 minutes, one verified refinement per run

1. light story — sky bloom behind the wordmark (the brand lights the night); central glitter lane + lane spec answer it
2. lip gleam — streamlines anchored to the lip (accelerating into the edge, fine second layer), tighter gleam line, flickering rim sparkles
3. overflow rivulets — two-octave finger spacing w/ real gaps, gravity stretch + accelerating shear, droplet breakup low down, sparkles clustered on rivulets. ALSO fixed v24 regression: glitter-lane spec referenced `path` before its declaration → GL program dead in v24/v25 (verify false-positive: console was read before a forced frame). Protocol updated.
4. ocean specular — distance-graded glitter: near = discrete stepped glints (tight pow-150 lobe), horizon = fine dense shimmer (140-cell hash, broader pow-80 lobe); fresnel lifts 25% toward the horizon for a more mirrored far field
5. atmosphere — second mist layer at its own scale/drift (sky parallax depth), near-wisp tint near the horizon, horizon glow now breathes (0.86–1.0 swell, ~14s, per-x phase so the pulse rolls along the line)
— loop stopped by Hasan ("pretty close"). Directed change: THICK water edge.
6. thick wave edge — lip is now a real rolling swell (4 traveling sines, ~±16px) and the water bends over it as a thick curling tongue (14–24px: bright rim, translucent aqua belly w/ subsurface glow, dark root, wrap streaks, sparkles); foam flecks race the last stretch to the edge; rivulets now pour from under the curl; JS lip mirror updated identically
— Hasan spec: scroll-driven cinematic with infinity-edge ending.
7. THE CINEMATIC (v31) — #flow pinned 260vh via GSAP ScrollTrigger scrub + Lenis glide; one bProg drives 4 phases: dolly+sway (0–.3) → services rise & drift past as perspective-projected buoys w/ bob+reflection, slipping over the edge ahead of you (.3–.55) → lip reveals from below frame, foam brightens, water accelerates (.55–.75) → the glide down: horizon+lip exit top, laminar face fills frame, 600-droplet stream, mist bloom, paper veil crossfades into services section (.75–1). New uniforms uCamZ/uCamX/uDesc/uLipF; de-snaked lip (longer swells, broken rim, dark glass curl); pixel ratio capped 1.5; mobile + reduced-motion + no-CDN all degrade gracefully (no pin, no descent). __rainProg(p) debug hook for verification.
8. fall rebuild (v32) — Hasan: ropes/arcs "very bad, make them real". Fall now: straight-down (pinch 0.0011→0.00022 — arcs were the #1 fake tell), 3 parallax layers at different speeds, pow-sharpened THIN filament bundles over dark translucent gaps, turbulence wobble growing with fall, aeration whitening, shedding droplet sparks, spray haze; killed grid-glint dashes. Artifacts: sky glyphs/constellations/streaks now fade with descent (globalAlpha), chip-glow dash row eliminated (basin--live added BEFORE measuring chips), wordmark exits fully with horizon. VERIFY TECHNIQUE (new, for frozen preview): extract SEA_FRAG via fetch+regex, compile in raw WebGL, render offscreen with phase uniforms, return ASCII luminance map — proves structure without screenshots.
9. four-bug pass (v33) — BUG1 freeze: engine now idles past the pin (onLeave/onEnterBack + veil-only short-circuit ≥0.995), cached soft sprite kills 26 per-frame radial gradients, GL fragment work capped by area (pr = min(1.5, sqrt(2.6M/px))), ScrollTrigger.refresh on resize+fonts; audited: no lenis.stop/overflow-hidden/wheel-preventDefault, no fixed canvases. BUG2 foam: noise-displaced smoothstep band (taper ~7.5px + spill), squared softness, shimmer, peak 0.35 pale cyan, sparks gated to band; curl rim 0.5→0.3, under-lip 0.3→0.2. BUG3 detail: two scrolling detail-noise layers (~6x scale apart, different drift) perturb the normal near-field only; near glitter cells 40→64; fall gets per-streak opacity variance + sin wobble + top-fade neck. BUG4 text: shadowBlur replaced by scaled cyan halo pass + crisp fill; reflection = flipped, 1.4x stretch, 8% alpha. Acceptance: full scroll down/up at 1380x860, resize to 1120x780, full scroll again — progress 0→1→0 both passes, max depth reached, center hit-test never lands on a canvas.
10. CALM REDESIGN (v34, Hasan spec) — cinematic deleted: no waterfall/lip/foam/descent, no pin/scrub (gsap+ScrollTrigger+Lenis removed from the page entirely), no water-mouse interaction (uMouseW/uRipples cut from shader = cheaper waveH), no text mirrors. Scene is ambient + self-animating on rAF: fixed camera with ~10s breath (bPar/bCamX sines), ocean runs to the bottom fade, services = boats at anchor in italic Cormorant Garamond — bob + roll ±2.3deg + pitch, slow sinusoidal drift (few units/min), near/mid/far at [[-30,100],[36,135],[-20,185],[52,240],[-52,300],[14,380]], staggered fade-in, never disappear; word click still scrolls to #services. Section is a normal ~88vh block. Acceptance: 14-step sweep down+up, zero stuck, back to 0, doc 2236px shorter (spacer gone), overflowX 0, shader compiles clean, fleet verified visually. rain.js 78k->61k.
11. fleet spacing + size (v35, Hasan: "very very good now") — anchors re-laddered near-to-far ([[-14,40],[17,55],[-10,320],[28,130],[-25,170],[-2,90]]): vertical span 69px → 187px, AI Automation now 46px low-left, Websites 41px low-right, Connected Systems 30px mid, Branding 16px far; fonts dy*0.21+8 (16..46, was 14..40); drift now depth-proportional (near ±~80px slow wander, far subtle) so the boats visibly MOVE; layout verified numerically (min pair dist 72, all in frame).
12. shoreline transition (v36) — 3-layer seam between ocean and services: (1) canvas bottom 15% dissolve via CSS mask-image on .sea/.basin; (2) SVG wave divider inside #flow (clamp 70..170px; 158px @1440, 70px @390): back wave #143246 (sampled from the real shader render @84% height), mid teal #46798f @0.5 drifting opposite directions (28s/21s translateX loops, transform-only, 2-cycle tiling paths, reduced-motion disables), front wave #faf8f5 (exact paper token) merging into the next section; (3) foam whisper on the front crest — 2px rgba(214,242,252,.25) stroke, blur(1px), vector-effect non-scaling. #services padding-top 116->clamp(150,15vw,200); fallback chips lifted above the wave. Verified: mask computed, 3 svgs animating, pointer-events none, overflowX 0 at 390/1120/1440, screenshot of the seam = soft. Zero JS.
13. REAL shore (v37) — Mike circled the SVG wave divider: "100% fake, low effort." Lesson confirmed again: flat vector fills can NEVER sit next to the rendered water. SVG divider + padding override deleted; the shore now lives IN the shader: noise-displaced lap line (~46px above bottom, sin push-pull 0.5/0.27 Hz ±8px), swash zone paling the water as it runs up, broken two-scale foam + twinkle at the line, backwash wisps below, alpha-to-paper; canvas mask kept at 92%. #services back to 116px. Verified via shore-zone luminance map: patchy foam clusters on a meandering line, organic dissolve, no continuous stroke.
14. minimal shore (v38, hard caps) — shore stripped to TWO elements: (1) single-smoothstep dissolve over bottom 12% into exact paper rgb(250,248,245), boundary displaced by ONE low-freq noise <=10px drifting ~12s/cycle; (2) waterline hint <=2px, peak 0.18, desaturated pale blue rgba-mix (0.72,0.8,0.86), broken by the SAME noise. Deleted: swash, all foam, twinkle, backwash, wisps, lap rhythms. Constants exposed at top of SEA_FRAG: DISSOLVE_HEIGHT 0.12 / LINE_OPACITY 0.18 / NOISE_AMPLITUDE 10.0. Acceptance: per-row delta-vs-paper strictly monotonic (188>158>119>74>36>9>0) — no local bump, nothing for the eye to stop on; seam screenshot = water ends, page begins.
15. seam strip killed (v39) — root causes: (a) dissolve END was noise-shifted so the gradient could complete below the canvas edge; (b) the old global bottom alpha-fade + .sea CSS mask made the last rows TRANSPARENT, exposing .dark-section ink behind = the dark strip; (c) navy->cream mid = neutral gray. Fixes: end pinned at uRes.y-6 (only the start breathes), alpha = mix(alpha,1,dissolve) so the bottom is OPAQUE exact paper rgb(250,248,245), global fade deleted, .sea mask removed (.basin overlay keeps its soft clip), mid routed through desaturated pale blue (0.78,0.85,0.9). Verified at two noise phases: bottom-row delta 0 / alpha 255 across all columns; mid B>R by ~28 (blue, not gray); DOM gaps 0.0px scene->flow->services.
16. haze quieted (v40, numbers only) — shoreMid darkened (0.78,0.85,0.9)->(0.63,0.68,0.71) (still blue>red, tinted toward cream), color ramp compressed via pow(dissolve,2.2) so near-white occupies only the last ~30% of the dissolve (long gentle ramp above), LINE_OPACITY 0.18->0.12. Shape/position/logic untouched; bottom row still exact opaque paper (delta 0, alpha 255). Profile: lum 59/62/97/187/247 at 86/89/92/95/97% — bright zone compressed, strictly monotonic, no eye-stop.
